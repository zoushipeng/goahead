#
#   ossl - run openssl command without trace
#

rm -f err.txt

echo "       [Run] openssl $*"
eval openssl $* 2>err.txt 
if [ $? -ne 0 ] ; then
    echo Command Failed
    cat err.txt
    exit 255
fi
rm -f err.txt
