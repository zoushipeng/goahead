/*
    doc.me -- MakeMe file for Appweb documentation
 */

Me.load({
    targets: {
        api: {
            action: `
                apidoc('api/goahead.dox', '${SRC}/src/goahead.h', 'GoAhead', 'api/*.tags')
                for each (let src: Path in ['api/goahead.html']) {
                    apiLayout(src, Path('out/ref/appweb').join(src))
                }
            `
        }
    },
})
