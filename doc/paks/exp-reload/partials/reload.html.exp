    <@ 
        let reload = expansive.services.reload
        if (reload && reload.enable && expansive.package.pak.mode == 'debug') { 
            addItems('scripts', 'lib/exp-reload/reload.js')
        }
     @>
