{
    platforms: ['linux-x86', 'linux-x64', 'macosx-x64', 'windows-x64'],
    steps:     ['prep', 'download', 'configure', 'build', 'test', 'package'],
    commands: {
        clean: 'me clean',
        configure: './configure --${profile}',
        build: 'me build',
        test: 'me test',
        package: 'me package',
    },
}
