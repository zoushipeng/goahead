{
    platforms: ['linux-x64', 'macosx-x64', 'windows-x64'],
    steps:     ['prep', 'download', 'configure', 'build', 'package', 'test'],
    commands: {
        clean: 'me clean',
        configure: './configure --${profile}',
        build: 'me build',
        test: 'me test',
        package: 'me package',
        publish: 'publish',
    },
}
